<h1>Listing product_params</h1>

<table>
  <tr>
    <th>Param</th>
    <th>Product Id</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_params.each do |product_param| %>
  <tr>
    <td><%= product_param.param %></td>
    <td><%= product_param.product_id%></td>
    <td><%= link_to 'Show', product_param %></td>
    <td><%= link_to 'Edit', edit_product_param_path(product_param) %></td>
    <td><%= link_to 'Destroy', product_param, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product param', new_product_param_path %>
