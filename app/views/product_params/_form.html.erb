<%= form_for(@product_param) do |f| %>
  <% if @product_param.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_param.errors.count, "error") %> prohibited this product_param from being saved:</h2>

      <ul>
      <% @product_param.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :param %><br />
    <%= f.text_area :param %>
  </div>
  <div class="field">
    <%= f.label :product_id, "Product Id"%><br />
    <%= f.text_field :product_id, size: 3%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
